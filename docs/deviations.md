# Deviations from the registered design

Log every departure from docs/design.md here with the date and the reason
(design document, Section 11). Leave the table empty until a deviation occurs.

| Date | Section | Deviation | Reason |
|---|---|---|---|
| 2026-09-12 | 3 | The secondary graduation outcome runs through school year 2020-21, not 2023-24. Its window is end years 2011 through 2021. | LEA-level adjusted cohort graduation rate files exist only through 2020-21. The ED Data Library's graduation listings for 2022-23 and 2023-24 hold state-level files only, and a state-level rate cannot form a within-district gap (Section 6). Consequence: reforms after 2020 have no post-reform years in either outcome and remain not-yet-treated controls. |
| 2026-09-12 | 3 | School year 2019-20 is retained for the graduation outcome. It stays excluded for the achievement gaps. | The Section 3 exclusion of 2019-20 applies to the waived assessments; graduation rates were reported that year, and the LEA file exists. |
| 2026-09-12 | 5 (rules 1, 2) | Graduation outcome: rules 1 and 2 are applied over end years 2010-2021, 2009-10 included. From 2014-15 the CCD LEA Directory has no `TYPE` or `BOUND`; `LEA_TYPE` (1-2 regular) stands in for `TYPE` and `UPDATED_STATUS` for `BOUND`, under the same codes (2, 6, 7 not operational; 5, 8 a change). | Author decision. v18 does not say how rule 2 reads the post-2014 CCD fields or which years the graduation stability test covers. `UPDATED_STATUS` uses `BOUND`'s codes 1-8; 2009-10 is included because the covariates and SAIPE rate are fixed there. `LEA_TYPE` 9 (specialized district, new in 2019-20) fails rule 1. |
| 2026-09-12 | 5 (rule 6) | Graduation outcome: rule 6 uses each event table's groups unchanged (excluded = a reform in 2005-2009), with cohorts coded over end years 2011-2021, so a state treated in 2010 or 2011 has no graduation pre-period and is left out. | Author decision. The graduation window starts a year after the achievement window, and v18 gives no separate exclusion rule for it; re-deriving exclusion for 2006-2010 would need treatment years the blinded tables do not supply for excluded states. |
| 2026-09-12 | 7 | Graduation outcome: the tested-count robustness weight is the 2010-11 cohort count in the gap's two groups, fixed across years; a district without a 2010-11 row is left out of the weighted models only. | Author decision. There is no 2009-10 graduation file, so the Section 7 weight (students tested in 2009-10) has no graduation counterpart; the first graduation year is the nearest fixed count. |
| 2026-09-12 | 7 | Graduation outcome: the regression-based secondary estimators control for CEP (district-year) and the four 2009 covariates interacted with year, without the test-replacement flag; step 6 runs them on all three event sets. | Author decision. The test-replacement flag describes the assessment regime and has no bearing on a graduation rate. Section 6 gives the graduation gaps the same event sets as the primary gaps. |
| 2026-09-12 | 4, 11 | The SY2018-19 (end year 2019) LEA achievement and participation files for mathematics and reading/language arts are taken from the Education Data Center EDFacts archive (`eddatacenter.org/edfacts`; ED's legacy long-layout files `*-lea-sy2018-19-long.csv`, `DATE_CUR` 13AUG20), not from ED's own ED Data Library release. Manifest rows `edfacts_*_edc_archive`, archived with checksums; data/manifest/README.md. | ED's release is truncated: its four ED Data Library files (`SY1819_FS175/178/185/188`) hold only Alabama through North Dakota, BIE and Puerto Rico, so 16 states (OH, OK, OR, PA, RI, SC, SD, TN, TX, UT, VT, VA, WA, WV, WI, WY) have no 2019 row; a fresh download was byte-identical and the legacy ed.gov names 404. The archive files cover all 50 states and DC and, for the 36 jurisdictions both carry, report every high school percentage identically to ED's release; the only differences are empty cells (count 0 or blank, value "."), which no rule admits. |
| 2026-09-12 | 5 (rules 1, 2) | Achievement gaps: from 2014-15 the CCD LEA Directory's `LEA_TYPE` stands in for `TYPE` and `UPDATED_STATUS` for `BOUND`, under the same codes as the graduation outcome (2, 6, 7 not operational; 5, 8 a change; `LEA_TYPE` 9 fails rule 1). Stability is tested in the achievement window years, end years 2010-2019 and 2021; the 2019-20 CCD file does not enter. | Author decision. The design does not say how rule 2 reads the post-2014 CCD fields. Rule 2 requires presence "in every year of the window", and 2019-20 is outside the achievement window (Section 3). Including 2019-20 would exclude 5 more districts, 3 of them high school districts with a SAIPE 2009 rate, all in Montana. |
| 2026-09-12 | 5 (rule 4) | Exact-only suppression sample: a cell whose participation is reported as the band GE95 passes the participation rule, as an exact value of 95 or more or GE99 does. Six cells in the full window: district 3819410 (ND) in 2014-15, math and RLA, all students and White; district 1722110 (IL) in 2017-18, math and RLA, all students. Step 3's check now stops only on a participation band that straddles 95. | Author decision. The rule for that sample (decision log, 2026-09-11) keeps exact participation values and names exact values and GE99 as what its cells report; the full window adds GE95, which the stage 1 files did not contain. GE95 cannot lie below 95, so it passes under the midpoint reading and the lower-bound reading alike. |
| 2026-09-12 | 7 (secondary estimators) | Achievement gaps, regression-based secondary estimators: CEP enters gaps (b) and (c) as the district's own district-year flag (the step 3 `cep` column: the state phase-in table before end year 2014, the district's CCD `NSLPSTATUS` from 2014), with `test_replaced` attached by district-year. For gap (a), a state-year outcome, CEP is the share of that state-year's gap (a) districts under CEP: quintile 1 or 5, a valid 2009-10 membership, retained under the event set, and the all-students cell usable in both math and RLA under rules 3 and 4 in the suppression sample (`gap_a_flags()`). `test_replaced` stays state-year. | Author decision. The decision of 2026-09-11 treated both controls as state-year flags, which held while CEP came from the state phase-in table (stage 1). From 2014 the `cep` column is district-level (data acquisition 2.5), so it differs within a state-year and step 6 could not form a state-year flag. The graduation outcome already takes CEP by district-year (entry above). The share keeps gap (a)'s control on the districts that form the outcome. |
| 2026-09-13 | 7 | The secondary estimators move to the unbalanced panel. Sun-Abraham, the imputation estimator, the stacked regression and the two-way fixed effects comparison rows run on the step 5 unbalanced panel for the achievement gaps and the graduation gaps, and those results are the main step 6 files (`outputs/06_secondary/`, `outputs/06_secondary/graduation/`). Synthetic difference-in-differences stays at the state level: its state-year means are taken over the unbalanced panel's districts, and the states with a mean in every window year enter (the rest are left out and counted). The balanced-panel versions of every estimator are written to `outputs/06_secondary/appendix/` (graduation: `appendix/graduation/`). This replaces the statement of Section 7 that the secondary estimators run on the balanced panel. | At the full window the balanced panel holds 69 districts in 9 states for gap (b) and 35 districts in 12 states for gap (c), too few for the Section 13 agreement table to serve its purpose, which is to show whether the primary estimate survives other estimators' weighting and comparison choices on the same sample. The unbalanced panel is the primary estimator's sample, so the table now compares estimators rather than estimators and samples at once. synthdid cannot take an unbalanced state-by-year matrix; restricting it to states observed in every year is the smallest change that keeps it at the state level (author decision, 2026-09-13). |
| 2026-09-13 | 7 (code correction) | Imputation estimator on the unbalanced panel: the custom weights for the event-time and overall estimands are built over the treated observations whose unit has at least one untreated observation, and the number of treated units left out is recorded in the model notes (`run_imputation()`). The balanced-panel results are unchanged. | A code error exposed by the move to the unbalanced panel. A treated district observed only after its state's reform has no unit effect to impute; `didimputation` drops those rows and rescales the weights, so the overall was no longer the equal-weight mean of the event-time estimates that the decision of 2026-09-11 defines (it differed by 0.0002 SD for gap (b) and 0.007 SD for gap (c)). Found by the step 6 output check in `tests/test_secondary.R`. |
| 2026-09-13 | 8 (code correction) | Randomization inference, tie handling. The two-sided p-value counts a placebo estimate as at least as far from zero as the observed one when it is within a relative tolerance of 1e-9 of it (`ri_pvalue()`, `R/functions/inference.R`). Step 7 was rerun at the registered counts for both outcomes; the estimates and draws are unchanged, and the p-values rose by at most 0.010 (5 achievement models) and 0.033 (6 graduation models). | A code error, not a change of rule: Section 8 defines the p-value from the reassignments whose estimate is at least as far from zero. A reassignment that reproduces the observed treated states gives the observed estimate again, but the refit can land a floating-point hair below it, and the strict comparison then left such ties out, making the p-value too small. The ties are frequent where an event set has a single cohort. Found when `tests/test_inference.R` rebuilt the p-values from the saved draws. |
| 2026-09-13 | 5, 7, 8 (code correction) | Test updates for the full window. The output checks in `tests/` that assumed the four-year stage 1 window now use the full window: a balanced-panel unit has an outcome in all 11 window years (`ACH_WINDOW`), not 4; a step 5 model on a small balanced panel may carry `did`'s "no valid att_gt() estimates" error as its status, while every unbalanced model and every primary-set unbalanced model must fit; a Romano-Wolf family exists only where at least two of its gaps have a step 5 fit, and the primary-set unweighted families must exist; the exact-only sample's participation check stops on a band straddling 95 rather than on any band wider than 1 point (the GE95 entry above). Reader tests were added for the long EDFacts layout and the gap (a) CEP share. | The tests encoded properties of the stage 1 files, not rules of the design; the rules they check are unchanged. |
| 2026-09-12 | 8 | Graduation outcome: the Romano-Wolf family is the two graduation gaps within an event set, weighting family and panel rule, apart from the achievement gaps. | Author decision. Section 6 calls the graduation gaps one secondary outcome family. |
| 2026-09-13 | 7 (anticipation) | Substitution. The robustness version that dates treatment to the initial lawsuit filing is replaced by `did`'s `anticipation = 1`, which dates treatment one year before the coded reform year and makes event time −2 the reference period (universal base period). The same model is the registered "reference at −2" version, so the two Section 7 anticipation checks become one. As implemented (`run_cs(anticipation = 1)`, `R/10_run_all.R`): event time −1 is estimated and reported as its own coefficient; the overall post-reform average stays `did` 2.5.1's mean of event times 0..+8; HonestDiD takes −2 as the reference, with −1 as a post-reference period carrying zero weight in `l_vec` (`honest_rm(ref = -2)`); a model whose estimated event times have gaps receives a status, not a bound. | Author decision. Filing dates exist only for court-driven reforms, so a filing-date treatment variable cannot be defined for statute events, and a filing-date table could not be permuted inside the blind: the permutation seed is held outside the repository and applies to the coded reform years only. |
| 2026-09-13 | 5, 7 (robustness variants) | The Section 5 and Section 7 robustness checks are each run as one departure from the primary specification (unbalanced panel, primary suppression sample, reference −1), on all three event sets and both weightings, at end years 2010–2019 and 2021 (graduation 2011–2021): ranges of 5 points or less; exact values only; end years 2013 on (achievement only; cohorts coded over 2013–2021, so a state treated in 2013 has no pre-period and is left out, and the fixed 2009–10 tested-count weight is kept); cohorts with fewer than three pre-reform window years dropped (their states removed from the panel); `anticipation = 1` (entry above). The balanced panel and weighted models are the step 5 models. Outputs: `outputs/10_run_all/variants/<outcome>/`. | Author decision. Sections 5 and 7 name each check without saying whether checks combine; one departure at a time isolates each rule, and running them on every event set and weighting keeps the Romano–Wolf families of Section 8 intact for every check. |
| 2026-09-13 | 8 (compute) | Randomization inference on the robustness variants uses 1,000 reassignments, not 10,000. The step 5 models (both panel rules, both weightings, three event sets) keep 10,000 in step 7. The Webb bootstrap and Romano–Wolf (9,999 draws) and HonestDiD (every M̄) run at the registered settings on every variant. Results are saved per model (`outputs/10_run_all/variants/<outcome>/ri/`, and for step 7 `outputs/10_run_all/cache/`) so an interrupted run resumes. | Compute. Each reassignment refits the estimator; 10,000 per model takes about 10 minutes, so the 123 variant models would add about 20 hours. With 1,000 reassignments the Monte Carlo standard error of a p-value near 0.05 is about 0.007. |
| 2026-09-13 | 9, 13 (Lee bounds) | Lee bounds are computed for gaps (b) and (c) only, at the district level, on the primary model (primary event set, unweighted, unbalanced). Tested share = the subgroup's tested count in year t (mean of math and RLA) divided by that subgroup's CCD grade 9 membership in year t − 3, summed from the school membership files to the district; where the race-by-grade count is unavailable the all-students tested count and grade 9 membership stand in. The differential change is the overall effect on the share from the same Callaway–Sant'Anna design (not-yet-treated controls, doubly robust, the four 2009 covariates). Each gap has two subgroups; the trimming fraction is the larger of the two absolute effects, capped at 1. The treated post-reform district-years of the primary panel are trimmed by that fraction (rounded to whole district-years) once from the top and once from the bottom of the outcome distribution, the primary model is refitted each way, and the two estimates are the bracket. Share years are end years 2013 on: the archived CCD membership files begin with 2009-10, so a state treated in 2013 has no share pre-period. Reading rules: a school's grade 9 all-students count is summed where reported; a district's race count is unavailable when any school with grade 9 students lacks that race's male or female count; in the long-format files (from 2016-17) a blank race-by-sex cell counts as zero when the school's reported cells sum to its grade 9 total (13,448 of 13,450 schools with a blank cell in 2017-18). No Lee bounds for gap (a) or the graduation gaps. | Author decision. Section 9 names the method and the enrollment ratios but not the share, the trimming fraction, or how trimming enters a Callaway–Sant'Anna estimate. For graduation, dropout is part of the outcome itself. Taking the larger subgroup effect gives the wider bracket. |
| 2026-09-13 | 7, 13 (dose scaling) | Dose scaling. Revenue per pupil = (TSTREV + TLOCREV) / V33 from the F-33 files (amounts in thousands of dollars, so thousands per pupil), FY = end year, deflated to 2021 dollars (CPI-U July–June, `deflate_to_base()`). First stage = the same Callaway–Sant'Anna model (not-yet-treated controls, doubly robust, unbalanced panel, the model's covariates) with revenue as the outcome, on the units of the gap's unweighted unbalanced model in every window year with an F-33 value. Gaps (b), (c) and the graduation gaps: district revenue per pupil. Gap (a): within each state-year, the 2009-10 membership-weighted mean revenue per pupil of quintile 5 districts minus that of quintile 1 districts, over the districts that can enter gap (a) (retained under the event set, valid 2009-10 membership). The dose-scaled effect is the ratio of the outcome's overall post-reform average to the revenue's (the Wald form of the registered two-stage estimate), computed for every gap and event set. Its interval is the 2.5–97.5 percentile interval of the ratio when the step 7 Webb draws (the same seed steps and state lists) perturb both estimates. When the revenue effect's own percentile interval includes zero, the ratio's confidence set is unbounded, and it is reported as unbounded. Revenue values are not trimmed. | Author decision. Section 7 registers two-stage least squares with the event-study indicators as instruments, which is a two-way fixed effects regression with the negative-weighting problem of Section 2; the ratio of the two Callaway–Sant'Anna overall effects is the just-identified form of that estimate on the primary estimator. Section 7 does not define revenue for a state-level gap. |
| 2026-09-13 | 13 (headline) | The honest-DiD bound sets are reported for all five M̄ values (0, 0.5, 1, 1.5, 2), and the bound set at M̄ = 1 is marked as the headline and gives the SD-unit interval in the answer to the research question. The per-$1,000 interval is the dose-scaled percentile interval. Graduation gaps receive every Section 13 item except Lee bounds. | Author decision. Section 13 asks for "an interval" without naming the M̄ value; M̄ = 1 allows post-reform departures from parallel trends as large as the largest pre-reform departure. |
| 2026-09-14 | 8, 11 (code correction) | Step 7 records the `fwildclusterboot` and `wildrwolf` versions in `inference_settings.csv` only when the packages are installed ("not installed" otherwise). `R/10_run_all.R` sets `RENV_CONFIG_SANDBOX_ENABLED=FALSE` for the processes it starts. | Two code errors exposed by the renv environment lock of 2026-09-13, found by the step 10 run. Step 7 stopped at its settings file because the lock leaves both packages out; the estimates were not affected. With renv's sandbox on, the 12 workers of steps 7 and 10 starting together queued on the sandbox lock, 11 missed future's connect timeout and the lock was left stale for later R processes. The renv project library still comes first, so package versions are those of `renv.lock`. |
| 2026-09-14 | 8 (code correction: HonestDiD grid) | The HonestDiD relative-magnitudes bound sets are read from a search grid that no reported bound set touches. `honest_rm()` starts each M̄ on HonestDiD's default grid (±20 standard deviations of the bounded overall average, 1,000 points). While a bound lies within half a step of an edge of the grid, the grid is widened on that side by its current width, with points added so that the step is unchanged, up to 6 times; a bound set still at an edge is given a status and no bound. Every M̄ row of `honestdid_overall.csv` carries `grid_lb`, `grid_ub` and `grid_points`. The bound sets were recomputed from the step 5 fits (`Rscript R/07_inference.R --honest-only`, both outcomes) and for the step 10 variants; the bootstrap, Romano–Wolf and randomization results were not rerun. Before and after: `outputs/07_inference/honestdid_grid_change.csv` (graduation: `graduation/`). | A code error. HonestDiD returns the lowest and highest accepted grid point, so a bound set wider than its default grid came back cut at the grid edge. The step 10 report showed the symptom: identical bound sets from M̄ = 0.5 to 2 (±0.427 for gap (b)). Cut sets understated the width of the registered primary result. |
| 2026-09-14 | 7 (data correction: dose first stage) | In the dose-scaling first stage, a district-year with F-33 fall enrollment (V33) below 30, or with state-plus-local revenue per pupil above $100,000 in 2021 dollars, is excluded before the revenue outcome is formed (district revenue per pupil for gaps (b), (c) and the graduation gaps; the quintile 5 − quintile 1 revenue gap for gap (a)). The dose-scaled estimates were recomputed. The district-years removed are counted, overall and within each first stage's districts, in `outputs/10_run_all/dose/<outcome>/revenue_exclusions.csv` and in the report. | Revenue per pupil reached about $7.4 million (2021 dollars) where a tiny enrollment count is the denominator; such values are artefacts of the denominator, not school finance, and in a district-level unweighted model a few of them can move the first-stage estimate. |
| 2026-09-14 | 9 (data addition: CCD 2006-07 to 2008-09) | The CCD school universe files for end years 2007, 2008 and 2009 are added to the manifest, downloaded and checksummed: 2006-07 `sc061c` v.1c, three fixed-width state-group files (`sc061cai_dat.zip`, `sc061ckn_dat.zip`, `sc061cow_dat.zip`) and its record layout `psu061clay.txt`; 2007-08 `sc071b` v.1b and 2008-09 `sc081b` v.1b, tab-delimited. The Lee tested shares now use grade 9 membership three years earlier for every achievement window year from 2010, so the share models reach the cohorts that the 2013-onward share window left out; the shares and the Lee bounds were recomputed. The fixed-width file is read at the layout's byte positions (school names carry single-byte accented characters). Replaces the share-year sentence of the 2026-09-13 Lee entry. | The archived CCD membership files began with 2009-10, so a tested share needing membership three years earlier started in 2013. NCES publishes the three earlier school universe files with the same grade-by-race fields. They are not outcome files, so the download stage gate does not apply; they are to be added to the OSF deposit. |
| 2026-09-14 | 9 (operationalization correction: Lee trimming fraction) | The Lee trimming fraction is the differential change in selection between the gap's two groups. For each group g (Black or Hispanic, and White), q_T,g is the treated post-reform tested share and q_C,g = q_T,g − ATT_g its counterfactual from the same Callaway–Sant'Anna fit; q_T,g is aggregated as `did` aggregates the overall effect (within each estimated event time 0..+8, cohort cell means weighted by the cohort's unit count; then the equal-weight mean over event times). p_g = 1 − q_C,g / q_T,g, and the trimming fraction is |p_minority − p_White|, bounded to [0, 1]. The treated post-reform district-years are trimmed by that fraction (rounded to whole district-years) once from the top and once from the bottom of the outcome distribution, where the differential selection occurs, and the primary model is refitted each way. Replaces the trimming-fraction sentence of the 2026-09-13 Lee entry. An intermediate rule of the same day (the larger of the two groups' relative fractions, trimming the control district-years when p < 0) is withdrawn. `lee_share_levels()`, `lee_p()`, `lee_differential_fraction()`, `lee_trim()`. | Author decision. Section 9 bounds the gap effect against "the differential change" in the tested population. The rule of 13 September set the fraction to the larger of the two groups' absolute effects on the tested share, so it trimmed on a common shift in testing, a change both groups share and one that moves the gap's composition little, rather than the differential change the design names; it removed 157 of 455 and 499 of 767 treated district-years for gaps (b) and (c). The intermediate relative rule kept the same flaw: in both gaps the two groups' p values had the same sign and nearly the same size, and it trimmed 2,316 of 8,462 and 3,179 of 8,801 control district-years. On the permuted tables the differential fraction is 0.0155 for gap (b) (p = −0.3768 Black, −0.3613 White) and 0.0832 for gap (c) (p = −0.5655 Hispanic, −0.4823 White), trimming 7 of 455 and 64 of 767 treated post-reform district-years. |
| 2026-09-15 | 13 (post-freeze reporting correction) | The Section 13 report builder (`build_report()`, `R/10_run_all.R`) tolerates a missing model: wherever a model's bootstrap, randomization, honest-DiD or estimate row is absent, the report cell carries the model's recorded status (from `model_status.csv`, e.g. "not estimable: error: No valid att_gt() estimates found ...") in place of its numbers; every cell whose row exists is unchanged. The step 10 log counts and lists the models so shown. No estimation code and no saved output was changed; the report was rebuilt with `Rscript R/10_run_all.R --report` from the saved outputs of run 20260914T210951Z, refitting nothing. | A code error in reporting, after the freeze. The full run on the real event table stopped at the report: the balanced-panel gap (b) model on the primary event set did not fit (`did`: no valid att_gt() estimates), so step 7 wrote no bootstrap, randomization or honest-DiD row for it, and the report's balanced-panel robustness row assumed those rows exist. Every model fitted in the rehearsal on the permuted tables, so the case did not arise before the freeze. |
| 2026-09-15 | 8 (post-freeze inference correction: HonestDiD on a reduced event-time block) | For every model whose HonestDiD status was "event times are not consecutive around the reference period" (step 7, both outcomes, all event sets and panel rules, and the step 10 robustness variants), the relative-magnitudes bound sets are computed on the largest consecutive block of estimated event times that contains the reference period and event time 0, with the post-reform average over the block's post-periods (equal weights), at every M̄, from the influence functions of the saved fits (`honest_block()`, `R/functions/inference.R`; `Rscript R/10_run_all.R --honest-block`). The block is recorded beside each bound set in `honestdid_overall.csv` (`event_block`, `event_block_note`) and in the report. Where the block has no pre-reform event time beyond the reference period, or does not reach event time 0, the status is left. The missing event times of each such model and why (no cohort, no district-year or state-year at that event time, or cells present but not estimable) are in `event_time_gaps.csv`, and the result per model in `honestdid_block_change.csv`, beside each `honestdid_overall.csv`. No estimate, bootstrap, Romano–Wolf or randomization result was changed and nothing was refitted. For these models the bounded quantity is the average over the block's post-periods, not the registered average over event times 0..+8, and the report says so beside each bound set. | Section 8 gives a model with no estimated pre-reform event time a status, but does not cover a model whose estimated event times have a gap, which HonestDiD cannot take (it reads the event-time estimates as consecutive periods). The rehearsal on the permuted tables did not produce the case; on the real event table it affects the primary gap (b) model, so the registered primary result of a bounds analysis was missing. Decided by the author after unblinding and before reading the estimates. |
| 2026-09-13 | 7 (secondary estimators) | Achievement step 6 runs on all three event sets, as the graduation pass does; `tests/test_secondary.R` now expects 54 models and requires every primary-set model to fit. | Step 10 instruction: steps 3 through 7 run for the primary event set and both robustness sets. |
