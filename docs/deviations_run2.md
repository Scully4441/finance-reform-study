# Deviations from the registered extension

Log every departure from docs/design_extension.md here with the date and the
reason (docs/design_extension.md, Section 13). The log is opened when the Run 2
registration is filed. Leave the table empty until a deviation occurs. Decisions
made before filing are in docs/decision_log_run2.md; Run 1's logs are
docs/decision_log.md and docs/deviations.md.

| Date | Section | Deviation | Reason |
|---|---|---|---|
| 2026-09-16 | 4 | Iowa 2022-2025 are excluded. The ISASP proficiency workbooks for all four years are archived (`hs_reportcard_ia`) as the record of the check. | The workbooks carry no race or ethnicity subgroup: each reports district by grade for all students only (sheets ELA, Math, Science). Iowa is a conditional state under Section 4, which enters only with district-by-race rows. |
| 2026-09-16 | 4 | Arkansas 2022 is excluded. The 2021-22 ACT Aspire "Reporting Categories, Avg Scale Scores, and Demographics" workbook is archived (`hs_reportcard_ar`) as the record of the check. | Race appears only at the state level: the Demographics sheet is statewide throughout, the Districts sheet is district by grade for all students, and the companion post-appeals summary file carries no race field. Arkansas 2022 is a conditional state-year under Section 4. |
| 2026-09-16 | 4 | Idaho 2025 is unavailable and does not enter. The manifest row `hs_reportcard_id` 2025 has no archived file. | No Idaho file for 2024-25 crosses race with grade. The Idaho Report Card export's `Student Group` is a single dimension, with race groups pooled over every tested grade and `High School` pooled over every race, and the 2024-25 legacy SDE workbook lists race groups and grades as alternatives in one `PopulationName` column. The high school result by race required by Section 4 cannot be formed. |
| 2026-09-16 | 4, 5 | Idaho 2022-2024 enter from the legacy SDE ISAT workbooks, archived from Wayback Machine copies (`hs_reportcard_id_sde`; captures 2022-11-08, 2024-06-01 and 2025-08-30, archived url and capture date in each row's `source`), not from the Idaho Report Card export, whose rows `hs_reportcard_id` 2022-2024 stay unavailable. The rows used are the district rows with Grade `High School` crossed with Population Black / African American, Hispanic or Latino and White; no row is labelled grade 11. The outcome enters as the published rates (Advanced plus Proficient). The 30-student floor applies to the tested count where the workbook prints one (`ProficiencyDenominator`, 2023 and 2024: 62 of 1,287 and 80 of 1,314 such rows) and Idaho's own minimum group size stands in for the floor wherever it does not (every 2022 row, which has no count column, and the remaining 2023 and 2024 rows). | Author decision. The SDE files are no longer live, and the export cannot meet the matching rule because it never crosses grade with race. The workbooks do cross district, grade and race for Idaho's high school ISAT, the test named in `data/reference/test_replacement.csv`, which names the grade as "high school grade" with no number. They publish rates with the count omitted for small groups, which is the case Section 5 covers by the state's minimum group size. |
| 2026-09-16 | 4 | Michigan 2022-2025 are unavailable and do not enter. All twelve MI School Data files are archived and checksummed as the record of the check, not as outcome data: the High School Assessments files (`hs_reportcard_mi`) and the SAT Math and SAT EBRW Proficiency Cross-tabulated Data files (`hs_reportcard_mi_math`, `hs_reportcard_mi_rla`), one of each per year. | The matching rule names grade 11 SAT Mathematics and SAT Evidence-Based Reading and Writing. For grade-11 ELA and mathematics the High School Assessments files hold only the alternate assessment (MI-Access); M-STEP covers science and social studies there, and 2024-25 adds grade 9 and 10 PSAT. The SAT cross-tabulated files carry grade 11 SAT, but give race only crossed with a second group (sex, English learner status or disability), with no race-alone row, no all-students row and no tested count, so district-by-race rates cannot be formed from them. |
| 2026-09-16 | 4 | The AZ, CO, DC, KY, MN, NH, NM and VA report-card files were downloaded through a browser and moved into `data/raw/hs_reportcards/`; `R/02_download.R` verifies their checksums but cannot fetch them, so a reader working from an empty `data/raw/` must fetch them by hand from the url or `source` recorded in each manifest row. | The sites refuse scripted requests: Cloudflare in front of azed.gov (AZ), a redirect to resources.finalsite.net with the name in Content-Disposition (CO), a Box share needing the share page's cookie (DC), 403 without a browser user agent (KY, NM), a Radware JavaScript challenge (MN), and 403 to any scripted client (NH, VA). The bytes are fixed by the recorded checksums. |
| 2026-09-16 | 4 | `R/02_download.R` checksums any manifest row with a blank `url` whose `filename` is present in `data/raw/`, and verifies it against the recorded `sha256`; a blank `url` is no longer a reason to skip a row. The change was made when the interactive-tool export part files were merged into the manifest (commit d7d2f3a). | The report-card files exported by hand from interactive tools, or delivered by email, have no url to fetch from. Without the change their checksums would be neither recorded nor verified. |
| 2026-09-16 | 4 | North Carolina's proficiency share is Level 3 and up (`pct_glp`, Grade Level Proficient) in `data/reference/hs_reportcard_harmonization.csv`, 2022-2025; `pct_ccr` (Levels 4-5) is not used. | Author decision. The NCDPI disaggregated files print both Grade Level Proficient (Level 3 and above) and College and Career Ready (Levels 4-5); Section 4 takes the state's federal accountability definition, and NCDPI's achievement indicator counts Level 3 and above. |
| 2026-09-16 | 4 | South Carolina's proficiency share is C or better on the EOCEP (`pcta` + `pctb` + `pctc`, scale score 70 and above) in `data/reference/hs_reportcard_harmonization.csv`, 2022-2025. | Author decision. The EOCEP district files report letter-grade percentages only; the harmonization draft had marked the cut unconfirmed. |
| 2026-09-16 | 4 | Georgia's mathematics result pools Coordinate Algebra with Algebra I where both are present (2022-2023, and in 2024 with Algebra: Concepts and Connections), using `NUM_TESTED_CNT` and `PROFICIENT_CNT` + `DISTINGUISHED_CNT`, in `data/reference/hs_reportcard_harmonization.csv`. How a pooled share is formed when a component's counts are suppressed (TFS) is not decided. | Author decision. The Georgia Milestones EOC files print Coordinate Algebra rows beside Algebra I, and the Run 1 high school mathematics result is the end-of-course mathematics tests together. |
| 2026-09-16 | 4 | New Jersey's reading/language arts result is the grade 9 NJSLA (ELA09 workbooks) in `data/reference/hs_reportcard_harmonization.csv`, 2022-2025; the archived NJGPA (grade 11) workbooks are not used. | Author decision. The NJSLA is the test named in test_replacement.csv; the NJGPA is the graduation proficiency assessment, first operational as a field test for the class of 2023. |
| 2026-09-16 | 4 | Utah's reading measure is the published English Language Arts result of Utah Aspire Plus (grades 9-10) in every year, 2022-2025, in `data/reference/hs_reportcard_harmonization.csv`. The Utah files print no separate reading component, so the 2022-2024 result may include the English test, which was removed from spring 2025; the UT 2025 reading flag in `data/reference/test_replacement.csv` stays 1. | Author decision. The decision was to use the reading component; the archived USBE workbooks (every sheet, every year) carry only English Language Arts, so the author chose the published ELA result as the reading measure in all four years. |
| 2026-09-16 | 4 | Rhode Island enters with the grade 11 SAT alone (RIDE portal export, `SAT_Math` and `SAT_ELA`) in `data/reference/hs_reportcard_harmonization.csv`, 2022-2025. In `data/reference/test_replacement.csv` the RI 2022 row is flagged 1 in both subjects as a source change, and the RI 2022-2025 assessment names are the grade 11 SAT alone. | Author decision. Run 1's EDFacts high school result for Rhode Island pooled the grade 11 SAT with PSAT 10; the report-card export carries no PSAT 10 rows, so the reported instrument changes in the first report-card year (data acquisition 3.2: a change in which instrument is reported is a replacement). |
| 2026-09-16 | 4 | New Hampshire and Wyoming tested counts, printed only as bands (NH `Total FAY Students`, e.g. '30 - 35'; WY `No. of Students Tested`, e.g. '270 - 279'), pass the 30-student floor on the band's lower bound, in `data/reference/hs_reportcard_harmonization.csv`, 2022-2025. | Author decision. Neither state prints an exact tested count, which Run 1 rule 3 requires; the lower bound guarantees at least 30 tested. |
| 2026-09-16 | 4 | North Dakota's ranged proficiency levels follow the Run 1 range rule in `data/reference/hs_reportcard_harmonization.csv`, 2022-2025: the proficiency share runs from the sum of the Proficient and Advanced lower endpoints to the sum of their upper endpoints, width and midpoint are taken from those printed endpoints, and the share enters at the midpoint when the width is 10 points or less and is missing otherwise. | Author decision. The NDDPI files publish every performance level as a range (exact where the endpoints are equal, otherwise bands of 5 or 10 points) and print no tested count. |
| 2026-09-16 | 5 | Run 1 row ME 2021 in `data/reference/test_replacement.csv` is corrected from 0 to 1 in both subjects: the assessment names become NWEA MAP Growth Mathematics and Reading, third year of high school, with evidence `documented` (Maine DOE sources, accessed 2026-09-16). The ME 2022 note no longer carries its CHECK. Run 1 outputs were produced with the earlier coding and are not rerun here. | Author decision. Maine DOE: when testing resumed in spring 2021, NWEA MAP Growth was the state assessment in mathematics and reading for grades 3-8 and the third year of high school, in place of the SAT at high school; a different test on a different scale is a replacement in the first year its result is reported. |
| 2026-09-16 | 5 | Run 1 rows VT 2019-2021 in `data/reference/test_replacement.csv` are corrected: the assessment names change from Smarter Balanced grade 11 to grade 9, with the Vermont AOE grade 9 FAQ added to the sources; `replaced_math`, `replaced_rla` and `replaced` are unchanged. The VT 2022 note no longer carries its CHECK. | Author decision. Vermont AOE moved the high school Smarter Balanced tests from grade 11 to grade 9 beginning in spring 2018, and the report-card files report grade 9; the grade move is not a change of test program or scale. |
| 2026-09-16 | 4 | Georgia 2024 mathematics in `data/reference/hs_reportcard_harmonization.csv`: Coordinate Algebra is pooled with Algebra I only where both tested counts (`NUM_TESTED_CNT`) are reported; where one of the two is suppressed (TFS) the single reported course is used; where both are suppressed the district-group mathematics cell is missing. The result is pooled with Algebra: Concepts and Connections as logged above. The TFS case for 2022-2023 is not decided. | Author decision. It settles the suppressed-count case left open by the Georgia pooling entry above for 2024, the mixed year. In the 2023-24 file 21 district-by-race cells carry an Algebra I or Coordinate Algebra row besides Algebra: Concepts and Connections; 5 have both old courses suppressed, 4 of them with a reported Algebra: Concepts and Connections result. |
| 2026-09-16 | 5 | Run 1 row VT 2018 in `data/reference/test_replacement.csv`: the assessment names change from Smarter Balanced grade 11 to grade 9, with the Vermont AOE grade 9 FAQ added to the sources; the coding is unchanged. The VT 2022 note now reads that the 2018-2021 names were corrected. | Author decision, completing the VT 2019-2021 correction above: AOE moved the high school Smarter Balanced tests to grade 9 beginning in spring 2018, the first year whose result the 2018 row describes. |
| 2026-09-16 | 6 | SEDA grade 3-8 pooling uses SEDA's unadjusted standard errors (`cs_mn_se_*`) for the precision weights and the pooled standard error, not the NAEP-linking adjusted ones (`cs_mn_se_adj_*`) (`R/functions/seda.R`, `R/11_seda_outcomes.R`). | Author decision. Section 6 names inverse squared standard errors without saying which of SEDA's two; the SEDA 2025.2 documentation (Step 6, Step 11) gives the unadjusted error as the one for within-state comparisons and uses it for its own gaps, and every SEDA gap here is within a district or within a state. |
| 2026-09-16 | 5 | For SEDA, Run 1 rules 1 and 2 are tested in the SEDA end years with an archived CCD LEA directory, 2010-2019 and 2022-2024 (`SEDA_RULES_WINDOW`); the 2009 and 2025 rows take the district's result. | Author decision. The SEDA window is 2009-2019 and 2022-2025, and the archived CCD LEA directories cover end years 2010-2024 only; no 2008-09 or 2024-25 file is in the manifest. |
| 2026-09-16 | 6 | SEDA gap (a) poverty quintiles are formed within state at the SAIPE 2009 rate among districts passing rules 1 and 2 (over `SEDA_RULES_WINDOW`) with a SAIPE 2009 rate and a 2009-10 CCD grade span (GSLO-GSHI) that includes a grade from 3 to 8; ungraded or missing span ends do not qualify. Run 1's quintile set, a 2009-10 span reaching grade 12, is not used for SEDA. | Author decision. Section 6 forms gap (a) "as in Run 1 Section 6", whose grade-span condition was set for the high school outcome; for grades 3-8 it would leave out elementary-only districts, which carry SEDA data (2,092 districts in the SEDA quintile set that the span-to-12 condition drops). |
| 2026-09-16 | 4, 6 | Report-card districts are matched to NCES LEAIDs through `data/reference/hs_reportcard_leaid_crosswalk.csv`, drafted by `R/12_hs_reportcard_outcomes.R` from the CCD LEA directories for end years 2022-2024: the state's district code against CCD `ST_LEAID` (method `st_leaid`), and for NH, RI, UT and WY, whose files print names only, a unique match of the normalized name within the state (method `name`; `ambiguous` and `unmatched` keys get no LEAID). Keys already in the table are never redrafted; `author_check` is left for the author, and the name matches are to be checked. | Author decision (step 12 decision 1). Section 6 appends the report-card years to the Run 1 panel, which is keyed by LEAID; the files carry state codes or names, and CCD `ST_LEAID` is the published link. No 2024-25 directory is archived, so matching uses 2022-2024. |
| 2026-09-16 | 5 | Rules 1 and 2 for the report-card years: Run 1's retained flags (rules 1, 2, 6 and 7 over 2010-2019 and 2021) stand unchanged, and a report-card row is also removed (retained 0 in every event set, `rc_rule12` naming the rule) when the district, in that year's CCD LEA directory, is not agency type 1 or 2, is not operational (`UPDATED_STATUS` 2, 6 or 7), shows a boundary change or reopening (5 or 8), or is not listed. End year 2025 takes the district's 2024 result. Districts absent from the Run 1 sample file are not retained. | Author decision (step 12 decision 2). Retesting rules 1 and 2 over 2010-2025 would drop districts that closed or changed after 2021 from the whole panel, and the EDFacts-years split of Section 10 would no longer reproduce Run 1. |
| 2026-09-16 | 5, 6 | Where no tested count is printed, the 30-student floor falls back to the state's minimum group size, recorded per state-year in the new harmonization column `min_group_size` (with `min_group_source`; drafted, the unverified values marked for the author's check: ID, KY, MN, MO, PA, WI, WY). A published rate is taken to meet that size. The same number is the count that clamps a share of 0 or 1 in V (`gap_v.R`); the binomial standard error is left blank unless both counts of the gap are exact. The rule is applied cell by cell: a cell whose count is suppressed while its rate is printed uses the minimum group size (as for Idaho, above), except where the suppression symbol itself states a count under 10 (GA `TFS`, NE masked counts, TN `*`, UT and WA `n<10`), which is read as a count band from 0 to 9 and fails the floor; NH and WY banded counts pass on the lower bound (above). | Author decision (step 12 decision 3), with the symbol reading as its application: `TFS` and the other symbols are statements that fewer than 10 students were tested, and Georgia prints percentages for such groups, so a printed rate there does not show that the group reached the minimum. |
| 2026-09-16 | 4, 6 | Pooled results (GA 2022-2024 mathematics, CO SAT grade 11 with PSAT grades 9 and 10, DC Algebra I with Geometry and Algebra II, NY 2024 Common Core with Next Generation Algebra I): the components whose tested count and rate are both printed are pooled with the counts; a single printed component is used alone; with none printed the cell is missing. This extends the Georgia 2024 rule above to GA 2022-2023, CO and DC. A component whose count is printed but whose rate is suppressed (GA prints counts of 1 to 5 with the rate `TFS`) counts as suppressed. | Author decision (step 12 decision 4). The Georgia entry above left the suppressed-component case open for 2022-2023; CO and DC present the same case. |
| 2026-09-16 | 4, 6 | Gap (a) from report cards uses the all-students row named in the new harmonization column `subgroup_all`. CO, CT, OH and RI print no all-students row in the archived files (`subgroup_all` blank) and have no report-card gap (a). The quintiles, the 2009-10 membership weight and the formation are Run 1's. | Author decision (step 12 decision 5). The harmonization table mapped only the Black, Hispanic and White rows. |
| 2026-09-16 | 5 | Participation is tested with the Run 1 95% rule (exact value or band midpoint) where the state prints a participation rate by group, recorded in the new harmonization column `participation_col` (AL, CO, CT, ID 2023-2024, IL, KS 2023-2025, LA 2025, MA, MO, NE, NH, NV, OR, RI, SD, TN, WA, WY); a cell with no printed value then fails. Where the file prints the complement (KS `Pct. Not Tested`, MO the level-not-determined percentage, NE `NOT_TESTED_PERCENT`, IL 2024 all students' `No Participation Rate`) the rate is 100 minus it. CO's pooled result takes the component rates weighted by `Number of Total Records`. Elsewhere `part_ok_*` is blank (not applicable). NE 2024-2025 counts as not printing participation: the files keep a `NotTestedPercent` column but mask it (`*` or `NA`) in all but 35 of the district-subject-group rows with a printed rate, and a masked not-tested count marks few non-testers, so failing those cells would drop them for the opposite reason. The printed value is read as a range with the Run 1 midpoint (`100.00%`, `>95`, `< 5.0%` parse as the other percentages do). Author to check the NE reading. | Author decision (step 12 decision 6); Section 5 and Section 11 (not applicable where not reported). |
| 2026-09-16 | 4 | `readxl` (1.5.0.1) and `readxlsb` (0.1.61, GitHub velofrog/readxlsb at commit 644ef7a; the CRAN archive 0.1.6 does not compile with the current toolchain) are added to `renv.lock`. `readxlsb` reads a whole Alabama sheet in hours, so `rc_xlsb_rows()` reads it in 2,000-row ranges. New York's Access databases are exported to CSV by `R/02a_ny_access_export.R`, run in the project container with mdbtools 1.0.0 (this machine's Access ODBC drivers are 32-bit and cannot be loaded by 64-bit R); the four CSV copies are archived beside the zips as manifest rows `hs_reportcard_ny_src_csv` 2022-2025 with their checksums and the command in `notes`. | Author decision (step 12 decision 7): no hand-saved copies; the conversion is scripted and reproducible. |
| 2026-09-16 | 7 | CEP in the report-card years: the district's CCD `NSLP_STATUS` for end years 2022-2024 (school characteristics files for 2021-22 to 2023-24, added to the manifest and archived); 2024-25 has no file and carries each district's 2023-24 value, flagged `cep_carried` = 1. | Author decision (step 12 decision 8). CEP participation is persistent, and a blank would drop the 2025 rows from the regression-based estimators. |
| 2026-09-16 | 6 | Output layout: `data/derived/hs_panel_run2.csv` is the Run 1 sample file (`sample_district_year.csv`, rows unchanged, `source` = `edfacts`) with the 2022-2025 report-card district-years appended (`source` = `report_card`), plus `cep_carried`, `rc_rule12`, `nbasis_*` (what the floor was applied to) and `nclamp_*` (the clamping count). The gaps are in `data/derived/hs_gaps_run2.csv`, one row per gap, unit-year, subject and suppression sample, Run 1's step 4 gaps (`edfacts`) with the report-card gaps (`report_card`). New York 2025 mathematics uses the Next Generation Regents Algebra I rows only. | Author decision (step 12 decision 9). |
| 2026-09-16 | 4, 5 | Vermont 2022-2025 do not enter, though mapped in `data/reference/hs_reportcard_harmonization.csv` and loaded by step 12. Its files report high school results only by supervisory union or supervisory district (`SchoolIdentifier` SU001-SU069), which the CCD lists as agency type 3, so every Vermont report-card row fails Run 1 rule 1 in `R/12_hs_reportcard_outcomes.R`. The union results are not mapped onto member districts. | Author decision. Mapping a supervisory union's result onto its member districts would repeat one value across several units, treating one observation as several independent ones. |
