# Author decisions before registration

Decisions the author made before the OSF registration is filed that change or
add to a rule in docs/design.md. Section numbers refer to that document.
Departures from the registered design after filing go in docs/deviations.md.

| Date | Section | Decision | Rationale |
|---|---|---|---|
| 2026-09-11 | 3, 11 | The registration end year of the analysis window is **2021** (school year 2020-21), the newest school year with LEA-level EDFacts achievement files for both mathematics and reading/language arts. Checked 2026-09-11 in three places, with no outcome file downloaded or opened (stage gate, `data/stage.txt` = 1). **ED Data Library** (`https://eddataexpress.ed.gov/download/data-library`, where ED has published the assessment files since SY2021-22): LEA-level performance and participation for both subjects for SY2020-21 (posted 2022-05-25); SY2021-22 carries LEA reading/language arts performance only, with no LEA mathematics file; SY2022-23 and SY2023-24 carry no assessment files at any level; SY2024-25 is not in the year filter. **Legacy ed.gov path** (`www.ed.gov/sites/ed/files/about/inits/ed/edfacts/data-files/`, HEAD requests only): the stage-1 names `math-achievement-lea-`, `rla-achievement-lea-`, `math-participation-lea-` and `rla-participation-lea-` return 200 for sy2012-13 through sy2017-18 and for sy2020-21, and 404 for sy2018-19, sy2019-20 and sy2021-22 through sy2024-25. **Urban Institute Education Data Portal** (`/api/v1/school-districts/edfacts/assessments/{year}/{grade_edfacts}/`, both subjects in one endpoint): years 2009-2018 and 2020, and the portal defines `year` as the fall of the school year, so SY2009-10 through SY2018-19 plus SY2020-21. The **ED Data Express Data Download Tool** was non-functional on the check date - no filter facets rendered and every query returned "Your query returned no results", including a control query for a year the Data Library carries - so it neither confirms nor rules out any school year and is not evidence here. Two consequences for step 2: SY2018-19 is in the Data Library but is served under no stage-1 file name at the legacy path, so the manifest needs a different URL for end year 2019; and the window is end years 2010-2021, with 2020 left out (no assessment collection under the COVID waiver) and 2021 kept only where participation is at least 95 percent. | Section 3 sets the last window year to the newest EDFacts release available on the registration date, and Section 11 enters it in the registration. SY2021-22 is the newest year ED has published any LEA assessment file for, but it has published only the reading/language arts one; a unit-year outcome is the mean of the two subjects and both are required (Section 6, and the step 5 rule), so a year with one subject cannot enter the window. SY2020-21 is therefore the newest usable release, and it is the same answer in all three sources that were working on the check date. |
| 2026-09-11 | 5 (rule 3), 9 | Percent proficient reported as a range no wider than 10 percentage points enters at the range midpoint. Wider ranges and suppressed cells stay missing, and the 30-student floor stays. Width and midpoint come from the printed endpoints, with one-sided labels closed at 0 and 100 (20-29: width 9, midpoint 24.5; GE95: width 5, midpoint 97.5). Robustness samples: exact values only (the rule as written) and ranges of 5 points or less. This replaces "reported as an exact value (not a range) ... Range-reported cells are treated as missing and are not imputed" in rule 3 and "No imputation" in the Section 9 table. In the stage-1 files EDFacts reports high school percent proficient as a whole number only for groups of more than 300 tested students; groups of 31-60 get 10-point ranges and groups of 61-300 get 5-point ranges (`outputs/03_sample/range_widths_by_bracket.csv`). | The exact-only rule left about 200 district-years per subject-year for gaps (b) and (c), all in the largest districts. A range midpoint is measurement error in the outcome, bounded at half the range width and unrelated to treatment, which does not bias a difference-in-differences estimate and only widens its interval. The error is not classical: it depends on where the true rate falls within its band and is larger in probit units at the tails, so the no-bias claim is approximate; the exact-only and 5-point samples are the check. |
| 2026-09-11 | 5 | Districts that pass rules 1 and 2 but have no SAIPE 2009 child-poverty rate are dropped for all three gaps. Section 5 has no such rule. | The SAIPE 2009 child-poverty rate defines the fixed poverty quintiles for gap (a) and is a pre-treatment covariate in every model, so a district without it cannot be placed in a quintile or enter the doubly robust adjustment. The 199 districts are mostly agencies SAIPE does not cover (192 are absent from the SAIPE file; 7 have no children 5-17), 73 of them in the primary event set. |
| 2026-09-11 | 5 (rule 4) | The 95 percent participation test applies to the exact value where one is reported and to the band midpoint where participation is banded, with the same printed-endpoint convention as rule 3: GE90 (midpoint 95) passes; 90-94 (92), 80-89 and lower bands fail; nothing reported fails. This replaces the earlier reading of the same day, under which the band's lower bound had to reach 95 and GE90 failed. The exact-only robustness sample keeps exact participation values: its cells report participation as an exact value or GE99, and GE99 passes. | The same logic as the midpoint rule, and it reduces the 2012-to-2013 discontinuity in sample composition rather than removing it. Under the lower-bound reading, districts with 31-60 tested students, whose top participation band is GE90, could not pass from 2012-13 on. The effective threshold depends on district size because of the band widths: districts with 31 to 60 tested students pass on GE90, districts with 61 to 300 fail at 90-94, and only the largest districts face the literal 95. |
| 2026-09-11 | 6 (gap (a)), 7 | The enrollment weight in gap (a), the enrollment-weighted mean district score by poverty quintile, is the district's total membership in the 2009-10 CCD LEA universe file (`MEMBER`), fixed across years. Section 6 does not name the enrollment measure or its year. | The weight is pre-treatment, so the reform cannot move it, and it matches the 2009 enrollment covariate in Section 7. Same-year grade 9-12 membership or tested counts would let the reform change the weights. |
| 2026-09-11 | 6 (gaps (b), (c)) | V for the racial gaps is Black minus White and Hispanic minus White: V = Φ⁻¹(p_Black) − Φ⁻¹(p_White) and V = Φ⁻¹(p_Hispanic) − Φ⁻¹(p_White). Reardon's convention is the reverse (White minus Black). | Section 6 fixes gap (a) as the top-poverty quintile minus the bottom-poverty quintile, which is disadvantaged minus advantaged. Ordering (b) and (c) the same way makes all three gaps point in the same direction: gaps are usually negative, and a positive effect means the gap narrowed. |
| 2026-09-11 | 6 (gap (a)) | A district without a valid 2009-10 CCD membership (`MEMBER` coded -1, -2 or -9, or 0) is left out of gap (a) and stays in gaps (b) and (c). In stage 1 this affects four California quintile-5 districts whose 2009-10 `MEMBER` is -9 (fails NCES quality standards) and is valid in later years: 0609810, 0623160, 0626760 and 0691134. 0623160 has about 16,000 students. | The weight is fixed at 2009-10 and pre-treatment. Filling from 2010-11 would let a possibly post-treatment value in for early cohorts, and filling from SAIPE ages 5-17 would mix two measures. Like the SAIPE rule, a district that cannot be weighted cannot enter the weighted mean. |
| 2026-09-11 | 7 | The Callaway-Sant'Anna models (step 5) leave out the assessment-replacement flag and CEP availability, which Section 7 names as the time-varying controls. The flags stay available for the transition-year robustness exclusion (Section 9) and for the regression-based secondary estimators (step 6). | did::att_gt conditions only on baseline covariates and cannot take time-varying controls. Residualizing the outcome on the flags before estimation would be a procedure the design does not describe. |
| 2026-09-11 | 6, 7 | The outcome in estimation is the mean of math V and RLA V in a unit-year (district-year for gaps (b) and (c), state-year for gap (a)). Both subjects are required: if either is missing, the unit-year is missing. | Section 6 says the subjects are averaged within district-year but does not say what happens when one is missing. Requiring both keeps the outcome the same measure in every unit-year; a one-subject year would mix measures. |
| 2026-09-11 | 7 | Unbalanced panel: a district (gaps (b), (c)) or state (gap (a)) enters a Callaway-Sant'Anna model with the outcome in any window year, and `did::att_gt` is called with `allow_unbalanced_panel = TRUE`. This replaces the balanced-panel rule of the same day, under which a unit needed the outcome in every window year; that rule is kept as a robustness model. Every step 5 model is therefore fitted twice, once under each rule, and a `panel` column (`unbalanced` = primary, `balanced` = robustness) names the rule in every step 5, 7 and 8 file and in the model key `gap.event_set.weighting.panel`. The unit-year requirement is unchanged: a unit-year still needs both subjects. Units dropped by each rule are counted in `outputs/05_primary/panel_counts.csv`. Two consequences are recorded rather than decided again: the fixed 2009-10 tested-count weight cannot be formed for a district with no first-window-year row, so such a district is left out of the weighted robustness models alone (`dropped_missing_weight`), as a district without a valid 2009-10 `MEMBER` is left out of gap (a); and step 6's secondary estimators stay on the balanced panel, which is the only one synthdid can take, so the Section 13 agreement table compares an unbalanced primary with balanced secondaries and step 5's balanced robustness models are the like-for-like comparison. Stage 1 (`outputs/05_primary/panel_counts.csv`, primary event set): gap (a) 38 states unbalanced against 36 balanced; gap (b) 1,509 districts against 806; gap (c) 1,589 against 773, with 299 and 495 of the unbalanced districts carrying no 2009-10 weight. | A sixteen-year window with year-by-year suppression would drop any district missing a single year. Rule 3 is applied cell by cell and year by year, so a district falls out of the balanced panel for one suppressed subject in one year out of sixteen; the surviving panel would be the districts large enough to report an exact or narrow-range percentage in every year of the window, which is not the sample Section 5 defines. The four-year stage 1 window already shows the size of the loss: the balanced rule keeps about half the districts with a usable gap, and the share falls as stage 2 adds years. The cost is that did estimates each ATT(g, t) from the units observed in the two periods it compares, its repeated cross-section doubly robust form, so the composition can change across the periods being compared and the estimate is no longer conditional on stable inclusion (Section 9); the balanced robustness model is the check on that, and in stage 1 the two rules move the overall estimate by up to a few hundredths of an SD on the primary event set and further on the narrow event sets. The placebo spread is wider on the unbalanced panel, so the step 8 minimum detectable effects rise. |
| 2026-09-11 | 7 | Covariates for gaps (b) and (c): log of 2009-10 CCD total membership (`MEMBER`, the gap (a) weight); the SAIPE 2009 child-poverty rate; and the Black and Hispanic shares of 2009-10 CCD school membership, summed within the district over schools reporting valid `BLACK`, `HISP` and `TOTETH` counts, all grades, as shares of `TOTETH`. A district without a valid value for any of them is left out of the gap (b) and (c) models. | Section 7 names the three characteristics but not their form. District size is heavily skewed, so in levels a few very large districts would dominate the propensity and outcome regressions. The Black and Hispanic shares cover the groups in gaps (b) and (c). Leaving out districts with a missing value follows the SAIPE rule. |
| 2026-09-11 | 7 (gap (a)) | Gap (a) is estimated with no covariates (`xformla = ~1`), so the doubly robust estimator reduces to unconditional difference-in-differences with not-yet-treated controls. | Gap (a) is a state-year outcome, and Section 7's covariates are district characteristics. With cohorts of one or two states, a state-level propensity model is close to perfect separation. |
| 2026-09-11 | 7 (weighting) | The tested-count weight for the gap (b) and (c) robustness models is the number of students in the gap's two groups (Black + White, or Hispanic + White) tested in 2009-10, averaged over math and RLA, fixed across years. It enters through did's `weightsname`. | Section 7 says the robustness model is weighted by the number of students tested in the two groups but does not name a year. The 2009-10 count is pre-treatment for every cohort that can be estimated, so the reform cannot move the weights, and it matches the fixed 2009-10 weight in gap (a). |
| 2026-09-11 | 7 (secondary estimators) | Controls in the regression-based secondary estimators (Sun-Abraham, imputation, stacked, two-way fixed effects): the test-replacement flag (`test_replaced`) and CEP availability (`cep`) enter every gap as time-varying controls. For gaps (b) and (c) the four 2009 covariates of the Callaway-Sant'Anna models also enter, each interacted with year (in the stacked regression, with sub-experiment-by-year). Gap (a) takes the flags only. synthdid takes no controls. | Section 7 names the flags as the time-varying controls and the 2009 district characteristics as covariates, but says how they enter only for the doubly robust Callaway-Sant'Anna model. Unit fixed effects absorb a fixed covariate, so covariate-by-year terms are the regression form of the conditional parallel-trends assumption the CS models make. The CS models cannot take the flags (entry above); these estimators can, so they follow Section 7 in full. The agreement table therefore compares estimators that differ in the flags as well as in weighting. |
| 2026-09-11 | 7 (synthetic DiD) | synthdid runs at the state level, cohort by cohort. Gaps (b) and (c) are first averaged to state-year, unweighted over the districts in the step 5 panel; gap (a) is already state-year. Each cohort's states are compared with the states not treated within the window (never treated, or treated after the window). Event-time estimates pool the cohorts' effect curves weighted by treated states; the pooled ATT weights each cohort's estimate by treated states times post-reform years (Clarke et al. 2023). Standard errors come from seeded placebo reassignment among the control states. | synthdid takes one adoption date and a balanced panel, and Section 7 describes it as reweighting control states, so the units are states. A later cohort is treated inside the window and cannot serve as a control over a whole cohort panel. |
| 2026-09-11 | 7 (stacked regression) | The five-year clean-control window: each cohort's sub-experiment covers event times -5 to +5 within the window years. Clean controls are states untreated through the cohort year plus five: never treated, treated after the window, or a later cohort treated more than five years later. The regression uses the corrective weights of Wing, Freedman and Hollingsworth (2024), sub-experiment-by-unit and sub-experiment-by-year fixed effects, and state clusters. Event times +6 to +8 have no stacked estimate. | Section 7 fixes a five-year clean-control window but not the event window or the weighting. A symmetric five-year window keeps every control clean over the whole sub-experiment. Unweighted stacking weights the sub-experiments by their control counts; the corrective weights give the treated-share weighted average of the cohort estimates. |
| 2026-09-11 | 7, 13 | The overall post-reform average for every secondary estimator is the equal-weight mean of its event-time estimates from 0 to +8 (0 to +5 for the stacked regression), the definition did::aggte uses for the step 5 overall. Standard errors come from each estimator's covariance of its event-time estimates: through custom weights in didimputation for the imputation estimator, and from the placebo replications for synthdid. Two-way fixed effects are also reported with a single post-reform indicator, as their own row. | Section 13's estimator-agreement table compares overall averages, which mean the same thing only if every estimator weights event times the same way. Each package's own ATT weights treated observations, cohorts or post-reform years differently. The static two-way fixed effects coefficient is the conventional estimate Section 2 warns about, so it is reported for comparison. |
| 2026-09-11 | 8 (wild cluster bootstrap) | The Webb-weight wild cluster bootstrap runs on the Callaway-Sant'Anna influence function rather than through `fwildclusterboot`. did returns one influence-function row per unit; the rows are summed within state and each of the 9,999 replications multiplies every state's sum by an independent Webb weight, which recentres the estimate on zero and gives the p-value and the critical value. This is did's own multiplier bootstrap with Webb weights in place of Mammen's. It is applied to the overall post-reform average and to every event-time estimate of all 15 step 5 models. | `fwildclusterboot::boottest()` takes an `lm` or `fixest` object and tests a coefficient of that regression. The primary estimate is neither: it is an average of doubly robust group-time effects, so there is no regression coefficient for the package to resample. The influence function is what the wild cluster bootstrap resamples in either case, and did exposes it, so this applies the method Section 8 names to the estimator Section 7 names. Summing the influence function within state and checking it against did's own clustered standard errors confirms the clustering (`tests/test_inference.R` checks the same identity against `fixest` on a least-squares fit, where the two agree exactly). The alternative, bootstrapping a `fixest` model so the package could be called, would have moved the primary inference onto a secondary estimator. |
| 2026-09-11 | 8 (multiplicity) | The Romano-Wolf step-down over the three gaps is computed on the bootstrap draws above rather than through `wildrwolf`. One set of state-level Webb weights is drawn per event set and used by every model in it, so the draws carry the dependence between the gaps; the step-down then orders the gaps by |t|, takes the maximum bootstrap statistic over the hypotheses not yet rejected, and makes the sequence monotone. The family is the three gaps within an event set. The primary family takes the unweighted model of each gap; a second family takes the tested-count-weighted models of gaps (b) and (c), with gap (a), a state-level outcome, unweighted in both. | `wildrwolf::rwolf()` takes a list of `fixest` or `lm` models, which the primary estimates are not, for the reason in the entry above. Sharing one set of state-level draws across the three gaps is what `wildrwolf` does internally, and it is what makes the adjustment valid when the gaps are correlated: the three gaps are measured on overlapping districts in the same states. Because the draws are shared, the unadjusted p-value in the Romano-Wolf table is by construction the bootstrap p-value in the bootstrap table, which `tests/test_inference.R` checks. Section 8 fixes the family as the three gaps but not the weighting; running the weighted family as well keeps the registered robustness check from being left without a multiplicity adjustment. |
| 2026-09-11 | 8 (randomization inference) | Each of the 10,000 reassignments draws the treated states at random from every state in the model's panel (never treated, treated after the window, or treated within it) and gives them the observed cohort years, keeping the number of states per cohort year; the estimator is rerun on each and the two-sided p-value is the share of reassignments whose overall average is at least as far from zero as the observed one. The reassignments are drawn in the parent process from `seed_for()` and then handed to the workers. | Section 8 says reform years are reassigned across states 10,000 times but not which states are eligible. Permuting the years only among the states that actually reformed would cap the number of distinct reassignments at the number of permutations of those years: with the narrow event sets of stage 1 that is 2 for `event_table_r1.csv` and 24 for `event_table_r2.csv`, so the p-value could not go below 1/2 or 1/24 and 10,000 draws would be impossible. Drawing the treated set from every eligible state gives the distribution of the estimate when reforms are randomly timed and randomly placed, which is the null Section 8 describes, and it supports 10,000 distinct draws for every event set. Drawing in the parent process keeps the result independent of how many workers run it. |
| 2026-09-11 | 8 (HonestDiD) | The relative-magnitudes bounds put equal weight on every estimated post-reform event time (`l_vec`), so the bounded quantity is the overall post-reform average as `did::aggte` defines it in step 5. The event-time estimates enter with the reference period dropped and their state-clustered covariance from the same influence function. A model with no estimated pre-reform event time other than the reference gets no bound and is recorded with that status rather than dropped silently; in stage 1 this is every model on `event_table_r2.csv`, whose one cohort reforms too early in the window to leave a pre-reform coefficient. The unadjusted confidence set is reported alongside the bounds. | Section 8 applies the restriction to the overall post-reform average, and equal weights over the estimated post-reform event times is the definition of that average used everywhere else in the pipeline (Section 13's agreement table depends on the estimators meaning the same thing by it). The relative-magnitudes restriction bounds post-reform violations of parallel trends by the largest pre-reform violation, so it needs at least one pre-reform coefficient; with none, no bound exists to report and saying so is more informative than an empty cell. A four-year stage 1 window leaves few pre-reform years, and the stage 2 window will leave more. |
| 2026-09-11 | 10 | The Section 10 power simulation runs on the three primary gaps of the primary event set, unweighted, each on its own step 5 estimation panel. Placebo reform years are assigned by the rule Section 8 already fixes for randomization inference: the treated states are drawn at random from every state in the model's panel (never treated, treated after the window, or treated within it) and given the observed cohort years, keeping the number of states per cohort year. The minimum detectable effect is the exact inversion of the placebo distribution: the critical value is the 95th percentile of the absolute placebo estimates, and the MDE is the smallest constant shift of those estimates that this test rejects with probability 0.80. The normal-approximation figure, (z_0.975 + z_0.8) x sd, is reported beside it. All 2,000 runs per gap are drawn from `seed_for("power")` in the parent process. Stage 1 result (`outputs/08_power/mde.csv`): 0.207, 0.260 and 0.167 SD for gaps (a), (b) and (c). | Section 10 fixes the 2,000 runs, the 80 percent power and the 5 percent two-sided test, but not the assignment rule or the formula that turns the placebo spread into an MDE. Section 10 and Section 8 describe the same reassignment, so reusing the Section 8 rule keeps the placebo distribution and the randomization-inference distribution the same object and the same code. The inversion is exact rather than approximate: adding a constant to the treated post-reform outcomes shifts every doubly robust ATT(g, t), and so their average, by exactly that constant, so the shifted placebo draws are the distribution of the estimate under an alternative of that size. It also makes no normality assumption, which matters here: with two estimable cohort states in the stage 1 window the placebo distribution has heavy tails, and the exact MDE and the normal approximation differ by up to 9 percent (gap (c), where the normal approximation is the larger). |
| 2026-09-11 | 10 | The Section 10 simulation reports two placebo scenarios, 2,000 runs each. **Observed-cohort run** (`outputs/08_power/mde.csv`, `seed_for("power")`): the reassignment rule of the row above, which gives the placebo runs the cohort count the stage 1 window happens to contain, two treated states. That count is a property of the four-year pre-period, not of the registered design, so this run describes the pre-period and is not the study's power calculation. **Ten-state run** (`outputs/08_power/mde_10states.csv`, `seed_for("power_10")`): ten placebo-treated states, cohort years drawn uniformly from 2011-2013, matching the treated states with post-reform data in the registered design. This is the power calculation, and it is the one Section 10's 0.10 SD rule is applied against. Stage 1 result: MDE 0.112, 0.110 and 0.0996 SD for gaps (a), (b) and (c), against 0.207, 0.260 and 0.167 in the observed-cohort run. The ceiling is exceeded for two of the three gaps and not for all three, so Section 10's criterion does not register the study as underpowered. **Projection** (`mde_projection` in the ten-state file): each ten-state MDE times the square root of the ratio of placebo to full-design post-reform state-years, the design side computed from the cohort years in `data/reference/event_table.csv` with the registration end year set to 2025 as a placeholder and 2019-20 left out of the count. It is supplementary and is not a power calculation. Note for the author: the event table carries twelve treated states with post-reform data by 2025, not ten, so the ten-state run simulates two fewer treated states than the design will have and the projection's design side uses all twelve; the count is `TEN_STATES` in `R/08_power.R`. | Section 10 fixes the 2,000 runs, the 80 percent power and the 5 percent two-sided test, but not how many states a placebo run treats, and the answer moves the MDE more than any other choice in the step: the MDE falls roughly with the square root of treated state-years, and going from two placebo-treated states to ten roughly halves it. A power calculation for the registered design therefore has to put the design's own number of treated states into the simulation; the two-state figure answers a different question, which is how much the pre-period alone could have detected, and both are reported so the two are not confused. The projection is a 1 / sqrt(N) rescaling and nothing more: it holds the outcome's variance, the number of control states and the panel's unit count fixed, none of which the registered window will hold fixed, and its design side rests on a placeholder end year, so it is labelled a projection in the file and left out of the console. |
| 2026-09-11 | 10 | The registered power calculation is the **twelve-state run** (`outputs/08_power/mde_12states.csv`, `seed_for("power_12")`, `TWELVE_STATES` in `R/08_power.R`): twelve placebo-treated states, cohort years drawn uniformly from 2011-2013, 2,000 runs, on the primary (unbalanced) step 5 panel. Twelve is the number of treated states the event table carries with post-reform data by the placeholder registration end year 2025, which the script checks against the table and warns if it differs. Section 10's 0.10 SD ceiling is applied to this run alone. The **ten-state run** (`mde_10states.csv`, `seed_for("power_10")`) and the **observed-cohort run** (`mde.csv`, `seed_for("power")`, two treated states) are kept as sensitivity runs and the ceiling is not applied to them; this replaces the ten-state run as the power calculation, decided the same day. `mde_12states.csv` carries the same columns as `mde_10states.csv`, the supplementary projection among them. Stage 1 result: MDE 0.152, 0.128 and 0.122 SD for gaps (a), (b) and (c) in the twelve-state run, against 0.156, 0.143 and 0.137 at ten states and 0.225, 0.300 and 0.245 in the observed-cohort run. **All three gaps exceed the 0.10 SD ceiling, so on the stage 1 files the Section 10 criterion reads underpowered and the study would proceed as a bounds analysis.** | Section 10 fixes the 2,000 runs, the 80 percent power and the 5 percent two-sided test but not how many states a placebo run treats, and that choice moves the MDE more than any other in the step. The count that belongs in a power calculation for the registered design is the design's own, and the event table carries twelve treated states with post-reform data by 2025, not the ten the earlier entry assumed; the ten-state run therefore simulated two treated states fewer than the design has and understated the treated state-years by about a sixth. Keeping all three runs keeps the earlier figure on the record and separates the three questions they answer: what the registered design can detect (twelve states), what ten states would detect, and what the four-year pre-period alone could have detected (two states). The stage 1 verdict is a property of the four-year window, not of the registered one: the twelve-state placebo runs buy about 24 post-reform state-years against the registered window's 68, and the projection column rescales the MDEs to 0.090, 0.076 and 0.072 SD on that ratio alone. The projection is not a power calculation and cannot lift the verdict; what it says is that the criterion is being applied to a window a third the length of the registered one, and that the switch to the unbalanced panel, which widened the placebo distribution, moved the one gap that had been under the ceiling over it: at ten states on the balanced panel the MDEs were 0.112, 0.110 and 0.0996 SD, so gap (c) passed and the criterion did not read underpowered. |
| 2026-09-11 | 10 | The registered power calculation is the **six-state run** (`outputs/08_power/mde_6states.csv`, `seed_for("power_6")`, `SIX_STATES` in `R/08_power.R`): six placebo-treated states, cohort years drawn uniformly from 2011-2013, 2,000 runs, on the primary (unbalanced) step 5 panel, with the same columns as `mde_10states.csv`. Six is the number of treated states the event table carries with post-reform data by the registration end year 2021 (the Section 3 row above), which the script checks against the table and warns if it differs. Section 10's 0.10 SD ceiling is applied to this run alone. The **twelve-state** (`mde_12states.csv`, `seed_for("power_12")`), **ten-state** (`mde_10states.csv`, `seed_for("power_10")`) and **observed-cohort** (`mde.csv`, `seed_for("power")`, two treated states) runs are kept as sensitivity runs and the ceiling is not applied to them; the twelve- and ten-state runs were made while the end year was still assumed to be 2025, and twelve is the treated-state count under that assumption, not under the registered end year. This replaces the twelve-state run as the power calculation, decided the same day. Stage 1 result: MDE 0.182, 0.168 and 0.154 SD for gaps (a), (b) and (c) in the six-state run, against 0.152, 0.128 and 0.122 (twelve-state), 0.156, 0.143 and 0.137 (ten-state) and 0.225, 0.300 and 0.245 (observed-cohort). **All three gaps exceed the 0.10 SD ceiling, so the Section 10 criterion reads underpowered and the study proceeds as a bounds analysis.** | The count that belongs in a power calculation for the registered design is the design's own, and the end year fixes it: a window ending in 2021 gives six treated states with post-reform data, not the twelve the event table carries by 2025. Simulating twelve would credit the registered design with twice the treated states it has and understate the MDE by about a fifth. The three earlier runs stay on the record because they answer different questions and because two of them were made under the 2025 end year that the Section 3 row replaces. The projection column moves the same way: the registered window now buys 29 post-reform state-years against the six-state placebo runs' 12, so the projection rescales the MDEs to 0.117, 0.108 and 0.099 SD rather than the 0.090, 0.076 and 0.072 it gave against a 2025 window. It is still not a power calculation and still cannot lift the verdict, but it no longer brings any gap under the ceiling either, so the underpowered reading no longer rests on the stage 1 window being a third of the registered one: with the registered window ending in 2021 the two windows are within a factor of two and a half of each other in post-reform state-years. |
