# Step 10: run_all. Not yet written.
# Spec: see the matching section of docs/design.md and the run-order table in CLAUDE.md.
# Source shared functions with: for (f in list.files("R/functions", full.names = TRUE)) source(f)
stop("R/10_run_all.R is a stub. Implement it against docs/design.md before running.")
